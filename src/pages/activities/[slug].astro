---
import Layout from '../../layouts/Layout.astro';
import { getCollection, render } from 'astro:content';

export async function getStaticPaths() {
  const activities = await getCollection('activities');
  return activities.map(activity => ({
    params: { slug: activity.id },
    props: { activity },
  }));
}

const { activity } = Astro.props;
const { Content } = await render(activity);
---

<Layout title={activity.data.title} description={activity.data.tagline}>
  <article class="container-narrow page">
    <a href="/activities" class="back-link">← 活動一覧</a>

    <div class="img-slot detail-hero">
      <img src={`/images/activities/${activity.id}.jpg`} alt="" onerror="this.remove()" />
    </div>

    <header class="detail-head">
      {activity.data.comingSoon && <span class="detail-soon">準備中</span>}
      <h1 class="detail-title">{activity.data.title}</h1>
      <p class="detail-tagline">{activity.data.tagline}</p>
      {!activity.data.comingSoon && activity.data.frequency && (
        <span class="detail-freq">{activity.data.frequency}</span>
      )}
    </header>

    {activity.data.comingSoon && (
      <p class="soon-note">
        この活動はまだ始まっていません。これから準備していく「予定」の場です。
        最新情報は <a href="/links" class="inline-link">SNS</a> でお知らせします。
      </p>
    )}

    <div class="prose detail-body">
      <Content />
    </div>

    <div class="detail-cta">
      <a href="/events" class="btn btn-primary">イベントを見る →</a>
    </div>
  </article>
</Layout>

<style>
  .page { padding: 2.5rem 0 2rem; }
  .back-link {
    font-size: 0.85rem;
    color: var(--color-muted);
    text-decoration: none;
  }
  .back-link:hover { color: var(--color-brand); }
  .detail-hero {
    aspect-ratio: 16 / 9;
    border-radius: 1.5rem;
    margin-top: 1.5rem;
    box-shadow: var(--shadow-soft);
  }
  .detail-head { margin-top: 2rem; }
  .detail-title { font-size: 2rem; color: var(--color-text); }
  .detail-tagline { color: var(--color-muted); margin-top: 0.5rem; font-size: 1.05rem; }
  .detail-freq {
    display: inline-block;
    margin-top: 0.9rem;
    font-size: 0.78rem;
    color: var(--color-brand-dark);
    background: var(--color-brand-light);
    padding: 0.25rem 0.8rem;
    border-radius: 999px;
  }
  .detail-soon {
    display: inline-block;
    margin-bottom: 0.6rem;
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.08em;
    color: var(--color-surface);
    background: var(--color-accent);
    padding: 0.25rem 0.8rem;
    border-radius: 999px;
  }
  .soon-note {
    margin-top: 1.8rem;
    padding: 1rem 1.2rem;
    background: oklch(62% 0.09 55 / 0.1);
    border: 1px solid oklch(62% 0.09 55 / 0.25);
    border-radius: 1rem;
    font-size: 0.9rem;
    line-height: 1.8;
    color: var(--color-text);
  }
  .inline-link { color: var(--color-brand); text-decoration: underline; text-underline-offset: 3px; }
  .detail-body { margin-top: 2.2rem; }
  .detail-cta {
    margin-top: 3rem;
    padding-top: 2rem;
    border-top: 1px solid var(--color-border);
  }
</style>
